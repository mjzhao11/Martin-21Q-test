# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Martin 21Q test' do
  
  use_frameworks!

  # Pods for Martin 21Q test
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Database'

  target 'Martin 21Q testTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Martin 21Q testUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
